'use server';

// eslint-disable-next-line @typescript-eslint/require-await -- Server Actions must use an async function
export async function runServerAction() {
  return 'Hello from the server!';
}
