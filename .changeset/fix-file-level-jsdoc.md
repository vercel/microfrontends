---
"@vercel/microfrontends": patch
---

Convert file level JSDoc to a regular block comment in schema types to prevent OpenAPI generators from incorrectly associating it with the `Config` interface declaration.
